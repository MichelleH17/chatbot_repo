// asynchronní funkce - používá se tam async
// asynchornní - vrátí promise("") a v něm bude string, promise je objekt a na něm jsou definované metody, např. then 
// máme metodu then a na něm callback -- TEST1().THEN(RES => {...}), další metody catch - odchytává volání promisy, 
// pro volání API
// synchronní vrátí string

async function test(arg) {

}

const test1 = async () => {}
